package com.example.app_gestion_presences.controller;

// Ancien contrôleur conservé uniquement pour les DTOs internes.
// Toutes les routes ont été migrées vers EnseignantController et EtudiantController.
// Ce fichier est intentionnellement vide de mappings HTTP.
public class EventController {
    // Routes migrées :
    // /speaker/**   → /enseignant/**  (EnseignantController)
    // /participant/** → /etudiant/**  (EtudiantController)
    // /secretary/**  → /secretariat/** (SecretariatController)
}
